when:
  - event: [push, manual]

labels:
  platform: windows/amd64

skip_clone: true

steps:
  - name: build
    image: mcr.microsoft.com/windows/servercore:ltsc2022
    commands:
      - echo "Hello World"
